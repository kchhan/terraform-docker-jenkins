# terraform-docker-jenkins

Use terraform to create a EC2 instance with docker installed. Docker will be used to run Jenkins. Jenkins will be configured with JCasC.

The purpose is to create an easily creatable and easily destroyable environment to use or test Jenkins with.
